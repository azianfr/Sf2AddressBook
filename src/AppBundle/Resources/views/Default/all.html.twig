{% extends '::base.html.twig' %}

{% block body %}
    <div class="container">
        <ul>
            {% for user in users %}
                <li><a href="{{ path("contact_show", {'id': user.id} ) }}">{{ user.email }}</a></li>
                {% endfor %}
        </ul>
    </div>
{% endblock %}